$(documnt).ready(function()) {

    var source = $("#genre-template").html();
    var template = Handlebars.complie(source);

    var source = $().html();
    var template = Handlebars.complie(source);

    var context = {
        genre: {
            type: "Action" 
        }
    }
}